write ui for views
add in coordinator as *onboarding example, not view controller*
add tab items custom
add navigation bar as in swiftui
add json parsing for the first view
add json parsing for the second view
add popping view for the second view
combine for the cart view (if empty - dont add buttons for plus, if got smth - add it)
content view dont need cause all the job for main coordinator (dont touch app coordinator - its just for the first screen)
change category view controller to swift ui screen
let it be with detail view as it is
change cart view controller to swiftui screen
add view model for dishes screen
in view models add json fetching
add json files 

+добавить плашки на dishes view (сделать нормальную сетку как надо)
+при нажатии на плашку показывать chosen dish view
+убирать при нажатии на крестик chosen dish view
+в chosen dish view передавать все необходимые данные
с тэгами надо что то делать
!задизайнить chosen view по нормальному
+с помощью координатора и таб вью (их надо совместить каким то образом) переходить по вьюхам корзины профиля поиска и обратно домой еще на вьюъу с категориями
+nav bar сделать одну для всех одинаковую (кроме detail dish view)

+добавить картинки на кнопки на первом экране
+распарсить джсон для первого экрана

распарсить джсон для тэгов
если все меню - показывать то то если салаты то показывать их

крестик и сердечко переместить на саму картинку
картинку блюда сделать побольше
поработать с текстом и его отображением

кнопка добавить в корзину действительно должна что то добавлять в корзину
при переходе на экран корзина там отображается уже выбранное

при нажатии на плюс увеличиваем количество
при нажатии на минус - уменьшаем
что делать с удалением - можно ли удалить?

почистить от лишних файлов и координаторов
на втором экране название титула такое же которое выбрано на первом экране было
